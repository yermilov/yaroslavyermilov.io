{
  "name": "yermilov-github-io",
  "private": true,
  "version": "0.0.0",
  "description": "Personal site of Yaroslav Yermilov — content, lab, talks.",
  "engines": {
    "node": ">=24",
    "pnpm": ">=9"
  },
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm --filter web build && pnpm --filter web postbuild",
    "preview": "pnpm --filter web preview",
    "start": "pnpm --filter web start",
    "lint": "pnpm --filter web lint",
    "typecheck": "pnpm --filter web typecheck"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
