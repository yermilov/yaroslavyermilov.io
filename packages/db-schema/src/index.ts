// Drizzle schema for yaroslavyermilov.io. Empty for now: the API ships as a
// ready-to-grow skeleton (task 59e55927). Add tables here, one file per domain
// re-exported from this barrel, when the first backend feature needs
// persistence. The three-place lockstep (Drizzle table -> migration -> any
// client schema) from juggernaut applies once tables exist.
export {};
